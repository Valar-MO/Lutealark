# Optional local smoke-test overlay. Production should use the Caddy overlay
# instead of publishing the plain HTTP frontend directly.
services:
  frontend:
    ports:
      - "${HTTP_PORT:-8080}:80"
