# Leave empty during local development; Vite proxies /api to localhost:3000.
# Set this to the deployed backend origin in production, without a trailing slash.
# Bundled Android builds require a pure HTTPS origin (no path, credentials, query, or hash).
VITE_API_BASE_URL=
