## 一、首页默认状态：Agent 对话入口卡片

**位置与尺寸**：位于周期主视图下方，占屏幕宽度约 88%，高度约 200–220dp，水平居中。卡片顶部与周期视图底部间距 16dp。

**整体轮廓**：四边大圆角（20dp），背景为比页面底色稍暖的米白色（如 `#FAF8F5`），带极微弱的弥散阴影（Y 偏移 3dp，模糊 10dp，透明度 6%），营造"可按压"的柔和感。边框为 1dp 的极淡暖灰色（`#EDE8E0`），黄体期时边框颜色自动变暖（淡琥珀色 `#F0D9B5`，不透明度 60%）。

**上半区域（约 55% 高度）**：

- **左侧**：左侧：Agent 头像，圆形，44dp，背景为柔和的蓝绿渐变（从 #A8D5BA 到 #7FB8A2），中心放置一个极简的白色粗线条云雀图标。

- **右侧文字区**：

    - 主标题："今天想聊聊什么？"（16sp，中等字重，深灰 `#2D2D2D`）

    - 副标题："我在这里，不急着要答案"（13sp，常规字重，中灰 `#8A8A8A`）

- 头像与文字整体左对齐，垂直居中于上半区域，左侧内边距 20dp。


**底部输入条（约 45% 高度）**：

- 一条贯穿卡片宽度的浅灰色胶囊形输入条（高度 44dp，圆角 22dp），背景 `#F0EDE8`，边框 1dp `#E5E0D8`。

- 左侧内边距 16dp 处放置麦克风图标（20×20dp，灰色 `#B0A99F`），为语音输入预留 UI 占位。

- 中间占位文字：“说点什么，或长按记录感受…”（13sp，灰色 `#B0A99F`）。

- 右侧内边距 12dp 处放置发送箭头图标（圆形按钮，32dp，主色调背景 `#7FB8A2`，白色箭头图标）。


**漂浮预设短语（卡片上方）**：

- 在卡片**上方 10dp 处**，水平排列几个标签式气泡按钮，从页面加载时从上方轻微弹入（Y 轴位移 10dp → 0，时长 350ms，弹性缓动 `cubic-bezier(0.34, 1.56, 0.64, 1)`）。

- 按钮样式：圆角矩形（圆角 14dp），背景为卡片同色系的稍深变体（如 `#F5F0E8`），文字 12sp，常规字重，深灰色。带 1dp 的 `#E8E2D8` 边框。

- 默认短语（根据使用频率动态排序，首次为固定顺序）：

    -  "有个任务启动不了"

	- "现在好烦"

	- "崩溃了"

	- "太吵了"

	- "这周难熬"

	- "带我呼吸"

	- "谢谢"

	- "任务做不动"

- 点击任意短语后，直接以该文字作为用户消息进入全屏对话界面并触发发送，无需再点击发送按钮。


**点击行为**：

- 点击卡片任意区域（除预设短语外）→ 进入全屏对话界面，输入框自动聚焦。

- 点击预设短语 → 直接进入全屏对话，该短语已填入并发送。

- 长按卡片（>600ms）→ 轻微震动反馈（若设备支持），弹出快捷菜单："记录感受" / "开始呼吸" / "查看周期"。


---

## 二、聊天开始后的全屏界面

### 2.1 顶部导航栏

**高度**：56dp，固定置顶，背景为页面底色（`#FAF8F5`）并带底部 1dp 的极淡分割线（`#EDE8E0`）。

**左侧**：返回箭头（24dp，深灰 `#2D2D2D`），点击返回首页。箭头右侧 12dp 处显示当前周期阶段小标签：

- 非黄体期：淡薄荷绿圆角标签（背景 `#E8F5E9`，文字 11sp，深绿 `#2E7D32`），如"卵泡期"

- 黄体期：淡琥珀色圆角标签（背景 `#FFF3E0`，文字 11sp，深橙 `#E65100`），如"黄体期 · 缓冲模式"


**中间**：页面标题 "Lutealark"（16sp，中等字重，深灰），旁显示线条云雀头像 。

**右侧**：一个"感受"快捷按钮（32dp 圆形，淡灰背景 `#F0EDE8`，中心一个波浪线图标），点击后从底部滑出感受记录面板（详见 2.5）。黄体期时此按钮颜色变暖。

---

### 2.2 消息列表区域

**布局**：占据顶部导航与底部输入区之间的全部空间，可垂直滚动。背景为极淡的暖白色（`#FAF8F5`）。

**滚动行为**：新消息到达时，列表自动平滑滚动到底部（时长 300ms，缓动 `ease-out`）。用户手动上滑浏览历史时，自动滚动暂停；用户滑到底部后恢复。

**日期分割线**：若对话跨天，在消息列表中插入一条居中的淡灰色文字："7月28日"（11sp，灰色 `#B0A99F`），上下各 12dp 间距。

---

### 2.3 用户消息气泡

**位置**：右对齐，右侧距屏幕边缘 16dp，左侧最大宽度为屏幕宽度的 70%。

**样式**：圆角矩形，圆角 16dp（左下角为 4dp 小圆角，形成"尾巴"效果），背景为主色调（`#7FB8A2`），文字白色。

**文字**：14sp，常规字重，行高 1.5。内边距：上下 10dp，左右 14dp。

**时间戳**：气泡下方 4dp 处，右对齐，显示发送时间如"14:32"（10sp，灰色 `#B0A99F`）。

**状态指示**：发送中显示一个极小的旋转圆点（12dp，主色调）；发送成功后圆点消失；若失败显示红色感叹号，点击可重发。

---

### 2.4 Agent 消息气泡（核心）

**位置**：左对齐，左侧距屏幕边缘 16dp，右侧最大宽度为屏幕宽度的 80%。

**整体容器**：圆角矩形，圆角 16dp（右下角为 4dp 小圆角），背景为白色，带 1dp 的 `#EDE8E0` 边框。内边距：上下 12dp，左右 16dp。黄体期时，边框颜色自动变为淡琥珀色（`#F0D9B5`）。

**5 段式结构渲染**（每条 Agent 回复严格按以下 5 段渲染，每段之间有 8dp 间距）：

表格

| 段落          | 视觉标签                                                                      | 内容样式                                                                                               |
| :---------- | :------------------------------------------------------------------------ | :------------------------------------------------------------------------------------------------- |
| **① 情绪确认**  | 顶部左侧一个**绿色圆角标签**（背景 `#E8F5E9`，文字 10sp，深绿 `#2E7D32`，内边距 2dp 8dp），文字如"情绪确认" | 正文 14sp，深灰，行高 1.6。首句固定为情绪命名+正常化，如"听起来你现在感到烦躁——这在黄体期是很常见的。"                                         |
| **② 医学解释**  | **蓝色圆角标签**（背景 `#E3F2FD`，文字 10sp，深蓝 `#1565C0`）                             | 正文 13sp，深灰。包含激素机制解释，如涉及知识库引用，在段落末尾以右上角标形式显示可点击的 `[1]` `[2]` 数字链接（蓝色 `#1565C0`，10sp）。点击后底部弹出引用详情卡片。 |
| **③ 缓冲建议**  | **橙色圆角标签**（背景 `#FFF3E0`，文字 10sp，深橙 `#E65100`）                             | 正文 14sp，深灰，中等字重。内容必须是具体到动作+时间的微行动，如"现在，允许自己只打开文档，写三个标题，限时 5 分钟。"                                   |
| **④ 环境微调**  | **紫色圆角标签**（背景 `#F3E5F5`，文字 10sp，深紫 `#6A1B9A`）                             | 正文 13sp，深灰。一句可立即执行的感官减负建议，如"如果周围太吵，先戴上耳塞，播放白噪音。"                                                   |
| **⑤ 下一步邀请** | **灰色圆角标签**（背景 `#F5F5F5`，文字 10sp，深灰 `#616161`）                             | 正文 13sp，灰色 `#616161`。开放性结尾，无完成压力，如"想聊聊是什么任务卡住了吗？或者先试试上面的步骤，随时回来。"                                  |

**标签与正文的间距**：标签底部与正文顶部间距 6dp。

**去羞耻化语言实时处理**：

- Agent 输出中若包含禁用词（"应该"、"必须"、"加油"、"你可以的"、"努力"、"坚持"），该词以**淡灰色删除线**显示（`#B0A99F`，文字中间一条细线），其正下方或紧邻处显示替换后的许可性表达（正常颜色）。

- 例如：`~~应该~~ → 可以` 或 `~~加油~~ → 我在这里`。

- 禁用词检测在前端渲染层完成，不修改后端返回的原始文本。


**黄体期语气视觉调整**：

- 检测到用户处于黄体期时，Agent 消息容器的边框颜色变暖（`#F0D9B5`）。

- 消息背景从纯白变为极淡的暖白（`#FFFBF5`）。

- 缓冲建议段落（橙色标签）的背景色自动加深一档（`#FFE8CC`），文字加粗提示。


**知识库交叉引用**：

- 若 Agent 回复涉及其他功能模块（如"如果你正在处理具体任务，可以看看任务降级工具"），该句以**可点击的文本链接**形式呈现（下划线，主色调 `#7FB8A2`）。

- 点击后弹出底部动作面板："打开任务降级" / "开始呼吸" / "记录感受"，用户选择后跳转至对应模块。


**情绪确认优先**：

- 当用户输入包含情绪关键词（烦、累、崩溃、难过等）时，Agent 第一条回复的情绪确认段落自动**展开并高亮**（绿色标签背景加深，文字区域左侧增加 3dp 的绿色竖线装饰）。

- 若用户连续表达情绪，Agent 跳过医学解释段落，仅显示情绪确认 + 缓冲建议 + 下一步邀请（三段式）。


---

### 2.5 感受记录面板（底部滑出）

**触发方式**：点击顶部导航栏右侧"感受"按钮，或用户在输入框输入情绪相关文字时，输入框上方自动弹出"快速记录感受？"的轻提示条。

**面板样式**：从底部滑出（Y 轴位移 100% → 0，时长 300ms，缓动 `ease-out`），占屏幕高度 45%，圆角 20dp（顶部左右），白色背景，顶部带一个 36dp 宽的灰色短横线（`#D0D0D0`，宽 36dp，高 4dp，圆角 2dp，居中）作为拖拽指示。

**内容布局**：

- **标题**："此刻的感受"（16sp，中等字重，深灰），顶部内边距 20dp，左对齐 20dp。

- **副标题**："可以多选，没有标准答案"（12sp，灰色），标题下方 4dp。

- **感受网格**：4 行网格布局，行间距 8dp，列间距 10dp，左右边距 20dp。

    - 第 1 行（负向-唤醒）：😣 烦躁 / 😰 焦虑 / 😠 愤怒 / 😵 懵

    - 第 2 行（负向-低落）：😔 低落 / 😫 疲惫 / 😶 麻木

    - 第 3 行（正向）：😌 放松 / 😊 平静 / 😄 开心 / 🤓 专注

    - 第 4 行（身体）：🤕 头痛 / 🧘‍♀️ 紧绷 / 🌀 超载 / 🩸 痛经

- **按钮样式**：每个感受为圆角矩形按钮（圆角 10dp，高度 40dp），默认背景 `#F5F0E8`，文字 13sp，深灰。选中后背景变为对应情绪色（如烦躁→淡红 `#FFEBEE`，平静→淡绿 `#E8F5E9`），文字加粗，并带 2dp 的对应深色边框。

- **自定义输入**：网格下方一个"➕ 自定义感受…"按钮，点击后展开单行文本输入框（圆角 8dp，高度 40dp，背景 `#F5F0E8`）。

- **提交按钮**：面板底部一个贯穿宽度的主按钮"记录感受"（高度 48dp，圆角 12dp，背景主色调 `#7FB8A2`，白色文字 15sp，中等字重）。点击后：

    - 面板下滑收起

    - 输入框上方显示 "+5 积分" 的淡绿色小标签（从上方飘入，停留 2 秒后淡出）

    - 若从 Agent 对话中触发，感受记录作为一条系统消息插入对话列表："你记录了：😫 疲惫、😰 焦虑"


---

### 2.6 预设短语栏（对话界面内）

**位置**：当对话列表为空（刚进入界面）或用户消息发送后 Agent 正在回复时，在输入框上方显示。

**样式**：横向滚动的短语栏，高度 44dp，背景透明。左右边距 16dp。

**短语按钮**：圆角胶囊形（圆角 16dp，高度 32dp），背景 `#F5F0E8`，边框 1dp `#E8E2D8`，文字 12sp，深灰。左右内边距 12dp。

**默认短语清单**（8 条，根据使用频率动态排序）：

- "有个任务启动不了"

- "现在好烦"

- "崩溃了"

- "太吵了"

- "这周难熬"

- "带我呼吸"

- "谢谢"

- "任务做不动"


**交互**：点击后立即发送该短语作为用户消息，短语栏在 Agent 回复完成后重新显示（根据上下文动态调整可见短语）。

---

### 2.7 输入区域（底部固定）

**整体高度**：最小 56dp，随文字输入自动增高（最大 120dp）。

**背景**：白色，顶部 1dp 分割线 `#EDE8E0`。

**布局**：

- **左侧**："+" 按钮（32dp 圆形，淡灰背景），点击后弹出快捷操作菜单（感受记录 / 呼吸训练 / 周期视图）。

- **中间**：多行文本输入框，背景 `#F5F0E8`，圆角 20dp，高度自适应（最小 40dp，最大 96dp），内边距 10dp 14dp。占位文字："说点什么…"（13sp，灰色）。

    - Enter 键发送，Shift+Enter 换行。

    - 输入框聚焦时，背景变为白色，带 1dp 主色调边框（`#7FB8A2`）。

- **右侧**：发送按钮（40dp 圆形，主色调背景 `#7FB8A2`，白色纸飞机图标）。输入框为空时，按钮为灰色禁用状态（`#D0D0D0`）；有文字时激活。


**安全检测轻提示**：

- 若用户输入包含危机关键词（如"想死"、"活不下去"），输入框边框立即变为淡红色（`#FFCDD2`），输入框上方弹出轻提示条："你现在安全吗？如果需要，我可以帮你联系支持资源。"（12sp，深红，背景 `#FFEBEE`），右侧带"查看资源"按钮。


---

### 2.8 Agent 思考/加载状态

**位置**：Agent 消息区域，左对齐。

**样式**：一个圆角矩形（16dp），白色背景，高度 40dp，宽度 72dp，内部三个圆点水平排列（每个 6dp，主色调 `#7FB8A2`）。

**动画**：三个圆点依次缩放（scale 1 → 1.3 → 1），间隔 200ms，形成波浪效果。下方显示淡灰色小字"缓冲带正在整理思绪…"（11sp）。

**过渡**：Agent 回复到达后，加载气泡以淡出动画消失（200ms），新消息从下方滑入（300ms）。

---

### 2.9 安全检测弹窗（三级）

**关注级（低落/绝望情绪）**：

- 不阻断对话，在 Agent 回复底部附加一条淡橙色横幅（高度 44dp，背景 `#FFF3E0`，圆角 8dp）："如果你愿意，可以和我多聊聊，或者试试呼吸练习。" 右侧带"开始呼吸"按钮。


**中危级（死亡愿望/暗示表达）**：

- 半透明黑色遮罩（不透明度 40%），中央弹出圆角卡片（圆角 20dp，白色背景，宽度 80%）。

- 卡片内容：

    - 标题："你现在并不孤单"（18sp，中等字重）

    - 正文："我听到你说感到绝望。这种感觉是真实的，也是暂时的。你愿意先给信任的人发一条消息吗？"（14sp，深灰，行高 1.6）

    - 按钮区：主按钮"拨打 12356 热线"（主色调背景，白色文字）+ 次按钮"给紧急联系人发消息"（边框按钮）+ 文本按钮"继续对话"（灰色）。

- 卡片从底部滑入，带轻微弹性。


**高危级（明确自杀意图+计划）**：

- 全屏覆盖，不可关闭。

- 背景为深靛蓝（`#1A237E`），中央白色文字：

    - "请先把手边的危险物品放远"（20sp，粗体）

    - "给可信任的人发一条消息，或拨打 12356"（16sp，常规）

- 底部一个巨大的主按钮"拨打 12356"（贯穿宽度，高度 56dp，圆角 12dp，绿色背景 `#2E7D32`，白色文字 18sp）。

- 按钮下方小字："全国统一心理援助热线，24 小时"（12sp，白色不透明度 70%）。

- 此页面不展开长篇对话，仅提供简短指令和热线。


---

### 2.10 知识库引用详情卡片

**触发**：点击 Agent 消息中的 `[1]` `[2]` 等引用标记。

**样式**：从底部滑出（占屏幕高度 35%），圆角 20dp（顶部），白色背景。

**内容**：

- 顶部标题："参考来源"（14sp，中等字重）

- 引用列表：每条引用显示知识库编号（如"03 周期激素机制"）、章节标题、摘录片段（12sp，灰色，最多 3 行）。

- 底部"关闭"按钮。